spring.application.name=log-system

# SQLite
spring.datasource.url=jdbc:sqlite:log_system.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


server.address=0.0.0.0
server.port=8080


# # Enable HTTPS
# server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=dict123
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=dictlog

server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD}
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=${SSL_KEYSTORE_ALIAS}

# Optional: change server port
# server.port=8080